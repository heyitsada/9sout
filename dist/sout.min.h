/*
 * Copyright (c) 2026 waxodium <waxodium@proton.me>
 * Licensed under the MIT License. See LICENSE detail from:
 *      https://github.com/waxodium/sout/
 */

// sout.min..h generated by cmin.py

#include <stdarg.h>
#include <limits.h>
#if defined(_WIN32)
#include <windows.h>
#else
#include <unistd.h>
#endif
typedef struct{char data[1024];int position;}OutputBuffer;typedef struct{int prefix;int zero_pad;int width;char specifier;}FormatTable;void write_chunk(const char*buf,int length){if(length<=0)return;
#if defined(_WIN32)
DWORD written;WriteFile(GetStdHandle(STD_OUTPUT_HANDLE),buf,length,&written,NULL);
#else
write(1,buf,length);
#endif
}void flush(OutputBuffer*buffer){if(buffer->position>0){write_chunk(buffer->data,buffer->position);buffer->position=0;}}void addchar(OutputBuffer*buffer,char c){if(buffer->position>=1023)flush(buffer);buffer->data[buffer->position++]=c;}FormatTable parse_format(const char**cursor){FormatTable format={0};(*cursor)++;while(**cursor=='#'||**cursor=='0'){if(**cursor=='#')format.prefix=1;else format.zero_pad=1;(*cursor)++;}while(**cursor>='0'&&**cursor<='9'){format.width=format.width*10+(**cursor-'0');(*cursor)++;}format.specifier=**cursor;return format;}void soutf(const char*format,...){OutputBuffer stream={.position=0};va_list arguments;va_start(arguments,format);for(const char*cursor=format;*cursor!='\0';cursor++){if(stream.position>=1023)flush(&stream);if(*cursor=='%'&&*(cursor+1)!='\0'){FormatTable spec=parse_format(&cursor);switch(spec.specifier){case's':{const char*text=va_arg(arguments,const char*);if(text==NULL)text="(null)";while(*text!='\0')addchar(&stream,*text++);break;}case'd':{int number=va_arg(arguments,int);char digits[sizeof(unsigned int)*CHAR_BIT];int p=0;unsigned int num;if(number==0){addchar(&stream,'0');}else{if(number<0){addchar(&stream,'-');num=(unsigned int)(-(number+1))+1;}else{num=(unsigned int)number;}while(num>0){digits[p++]=(num%10)+'0';num/=10;}while(p>0)addchar(&stream,digits[--p]);}break;}case'u':{unsigned int number=va_arg(arguments,unsigned int);char digits[sizeof(unsigned int)*CHAR_BIT];int p=0;if(number==0){addchar(&stream,'0');}else{while(number>0){digits[p++]=(number%10)+'0';number/=10;}while(p>0)addchar(&stream,digits[--p]);}break;}case'x':case'X':{unsigned int number=va_arg(arguments,unsigned int);unsigned int original=number;char digits[sizeof(unsigned int)*2];int p=0;if(number==0){digits[p++]='0';}else{while(number>0){unsigned int digit=number%16;if(digit<10)digits[p++]='0'+digit;else if(spec.specifier=='x')digits[p++]='a'+(digit-10);else digits[p++]='A'+(digit-10);number/=16;}}int prefix=spec.prefix&&original!=0?2:0;int padding=spec.width-p-prefix;if(padding<0)padding=0;if(spec.zero_pad){if(prefix){addchar(&stream,'0');addchar(&stream,spec.specifier);}while(padding-->0)addchar(&stream,'0');}else{while(padding-->0)addchar(&stream,' ');if(prefix){addchar(&stream,'0');addchar(&stream,spec.specifier);}}while(p>0)addchar(&stream,digits[--p]);break;}case'c':{addchar(&stream,(char)va_arg(arguments,int));break;}case'%':{addchar(&stream,'%');break;}default:{addchar(&stream,'%');addchar(&stream,*cursor);break;}}}else{addchar(&stream,*cursor);}}flush(&stream);va_end(arguments);}
#ifndef SOUTF_H
#define SOUTF_H
void write_chunk(const char*buf,int length);void soutf(const char*format,...);
#endif
#include <stddef.h>
#include <string.h>
int soutspn(const char*src,const char*start,const char*end,char*destination,size_t cap){if(!src||!start||!destination||cap==0)return 0;const char*a=strstr(src,start);if(!a)return 0;a+=strlen(start);const char*b=end?strstr(a,end):a+strlen(a);if(end&&!b)return 0;size_t len=(size_t)(b-a);if(len>=cap)return 0;memcpy(destination,a,len);destination[len]='\0';return 1;}
#ifndef SOUTSPN_H
#define SOUTSPN_H
#include <stddef.h>
int soutspn(const char*src,const char*start,const char*end,char*destination,size_t cap);
#endif
